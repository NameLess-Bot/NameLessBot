generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mysql"
    url      = env("DATABASE_URL")
}

model Permissions {
     key Int @id @default(autoincrement())
     type Int
     permissionLevel Int
     serverId Int
}

model CommandsData {
    key Int @id @default(autoincrement())
    userId Int
    data String
    serverId Int
}