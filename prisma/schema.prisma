generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Permissions {
  type            Int
  permissionLevel Int
  serverId        BigInt
  id              Int    @id @default(autoincrement())
  userRoleId      BigInt
}

model CommandsData {
  userId   BigInt
  data     String
  serverId BigInt
  id       Int    @id @default(autoincrement())
}
